TOTAL_USERS = 3500       # Max concurrent Locust users; sets workload_norm scale in both sim and real env
CONTAINER_CPU_CORES = 2.0  # CPU cores allocated per Docker container (nano_cpus = CONTAINER_CPU_CORES * 1e9)
